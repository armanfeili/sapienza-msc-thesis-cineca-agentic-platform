"""
Performance test package.

These tests are lightweight micro-benchmarks intended to run quickly in CI.
They focus on relative performance regressions rather than absolute numbers.

You can run only these tests with:
    pytest -m performance
"""
# This module intentionally left simple; shared helpers can be added here later.

__all__: list[str] = []
